<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.elm.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.elm.entity.User">
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="username" property="username" jdbcType="VARCHAR" />
        <result column="password" property="password" jdbcType="VARCHAR" />
        <result column="email" property="email" jdbcType="VARCHAR" />
        <result column="avatar" property="avatar" jdbcType="VARCHAR" />
        <result column="register_date" property="registerDate" jdbcType="TIMESTAMP" />
        <result column="bio" property="bio" jdbcType="VARCHAR" />
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP" />
        <result column="wechat_account" property="wechatAccount" jdbcType="VARCHAR" />
        <result column="qq_account" property="qqAccount" jdbcType="VARCHAR" />
        <result column="is_logged_out" property="isLoggedOut" jdbcType="BOOLEAN" />
    </resultMap>

    <select id="getUserByWechatAccount" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT *
        FROM users
        WHERE wechat_account = #{wechatAccount}
    </select>

    <select id="getUserByQqAccount" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT *
        FROM users
        WHERE qq_account = #{qqAccount}
    </select>

    <select id="getUserByAccount" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT *
        FROM users
        WHERE username = #{username}
    </select>

    <insert id="createUser" parameterType="com.elm.entity.User">
        INSERT INTO users (
            username, password, email, avatar, register_date, bio, update_time, wechat_account, qq_account, is_logged_out
        ) VALUES (
                     #{username}, #{password}, #{email}, #{avatar}, #{registerDate}, #{bio}, #{updateTime}, #{wechatAccount}, #{qqAccount}, #{isLoggedOut}
                 )
    </insert>

    <update id="updateUser" parameterType="com.elm.entity.User">
        UPDATE users
        <set>
            <if test="username != null">
                username = #{username},
            </if>
            <if test="password != null">
                password = #{password},
            </if>
            <if test="email != null">
                email = #{email},
            </if>
            <if test="avatar != null">
                avatar = #{avatar},
            </if>
            <if test="registerDate != null">
                register_date = #{registerDate},
            </if>
            <if test="bio != null">
                bio = #{bio},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="wechatAccount != null">
                wechat_account = #{wechatAccount},
            </if>
            <if test="qqAccount != null">
                qq_account = #{qqAccount},
            </if>
            <if test="isLoggedOut != null">
                is_logged_out = #{isLoggedOut},
            </if>
        </set>
        WHERE id = #{id}
    </update>

</mapper>